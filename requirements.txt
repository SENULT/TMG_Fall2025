# TMG Fall 2025 - Requirements
# Python >= 3.8

# Core dependencies
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.2.0
pyyaml>=6.0

# Data storage
pyarrow>=10.0.0  # For parquet
fastparquet>=2023.0.0

# Text processing
regex>=2023.0.0
unidecode>=1.3.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# Version control & tracking
dvc>=3.0.0
dvc[s3]>=3.0.0  # Optional: for S3 remote storage
dvc[gs]>=3.0.0  # Optional: for Google Cloud Storage
dvc[azure]>=3.0.0  # Optional: for Azure Storage
wandb>=0.15.0  # Weights & Biases
mlflow>=2.8.0  # MLflow (alternative to W&B)

# Jupyter
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.25.0
ipywidgets>=8.0.0

# Vietnamese NLP (optional but recommended)
pyvi>=0.1.0  # Vietnamese word segmentation
underthesea>=6.0.0  # Vietnamese NLP toolkit

# Utilities
tqdm>=4.65.0
python-dotenv>=1.0.0
